apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: pipelinerun-ritika-pvc
spec:
  storageClassName: skills-network-learner
  resources:
    requests:
      storage:  1Gi
  volumeMode: Filesystem
  accessModes:
    - ReadWriteOnce